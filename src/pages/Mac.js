import React from "react";

function Mac() {
  //          유저          react libraray가 바꿔줌
  //JSX : JavaScript XML -> HTML 변환-> 화면배치

  return (
    // root tag는 하나만 존재해야 함
    <div>
      <p>Mac 화면입니다</p>
    </div>
  );
}

export default Mac;
